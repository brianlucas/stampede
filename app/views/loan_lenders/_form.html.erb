<%= form_for(@loan_lender) do |f| %>
  <% if @loan_lender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan_lender.errors.count, "error") %> prohibited this loan_lender from being saved:</h2>

      <ul>
      <% @loan_lender.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
