<%= form_for(@loan_servicer) do |f| %>
  <% if @loan_servicer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan_servicer.errors.count, "error") %> prohibited this loan_servicer from being saved:</h2>

      <ul>
      <% @loan_servicer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
